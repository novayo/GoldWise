import { View, Text } from 'react-native';
import styles from '../share/globalStyle';

export default function Post() {
    return (
        <View style={styles}>
            <Text>Post</Text>
        </View>
    );
}